steps:
# Install
- name: 'gcr.io/cloud-builders/npm'
  args: ['install']
# Build
- name: 'gcr.io/cloud-builders/npm'
  args: ['run', 'build', --prod']
# Deploy
- name: 'gcr.io/$PROJECT_ID/'
  entrypoint: 'bash'
  args:
  - 'if [ $BRANCH_NAME != \'production\' ]; then firebase -Deploy. ; fi'