steps:
# Install
- name: 'gcr.io/cloud-builders/npm'
  args: ['install']
# Build
- name: 'gcr.io/cloud-builders/npm'
  args: ['run', 'build', '--prod']
# Deploy
- name: 'gcr.io/$PROJECT_ID/npm'
  args: 
     -'if [ $BRANCH_NAME != 'master' ]; then -firebase -Deploy ; fi'